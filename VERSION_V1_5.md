# 섹시한 꾼만두 TV 메뉴판 V1.5

최종 확정일: 2026-09-14

## V1.5 확정 변경

- 만두 메뉴에 `치즈만두`를 추가했다.
- 치즈만두 가격은 `7,000`, 설명은 `1인분 6개`로 표시한다.
- 치즈만두에 `신메뉴` 태그를 추가했다.
- 신메뉴 태그는 인기 태그와 같은 크기·각도·기본 위치를 유지하고, 두 페이지 모두 아래로 2px 내려 배치한다.
- 신메뉴 태그는 밝은 노란색 배경(`#f4c542`)과 짙은 글자색(`#3a2b00`)을 사용한다.

## 유지 사항

- V1.4의 메뉴·사진·가격 정렬·색상·테두리·그림자·레이아웃·폰트 크기는 유지한다.
- 냄비우동 리터치 사진과 음료수의 면 메뉴 이동 상태를 유지한다.
- A4 한국어·외국어 메뉴판은 기존 V1 승인본을 변경하지 않는다.

## 기준 파일

- 메뉴 데이터: `content/menu-data.js`
- 렌더링: `src/app.js`
- 스타일: `src/styles.css`
- 버전 메타데이터: `menu-version.json`

Git 태그 `v1.5`는 이 확정 상태를 가리킨다.
