# 섹시한 꾼만두 TV 메뉴판 V1.2

최종 확정일: 2026-08-23

## V1.2 확정 변경

- 옛날 탕수육 가격을 `소 8,000` / `대 12,000`으로 구분했다.
- 유린 탕수육 가격을 `소 9,000` / `대 14,000`으로 구분했다.
- 두 가격은 기존 가격 영역 안에서 위·아래 두 행으로 표시한다.
- `소`는 위쪽, `대`는 아래쪽에 배치하고 네 라벨의 시작선을 동일하게 맞췄다.
- 가격 숫자의 오른쪽 끝은 기존 가격 열의 끝선에 맞췄다.
- 가격 숫자는 1페이지 28px, 2페이지 27px이다.
- `소/대` 라벨은 가격 숫자의 60% 크기로 표시한다.

## 유지 사항

- 메뉴 카드·사진·메뉴명·행 높이·다른 메뉴 가격 위치는 V1.1과 동일하다.
- 1페이지는 30초 표시 후 2페이지로 전환한다.
- 2페이지 동영상 종료 후 1페이지로 복귀한다.
- 일반 버전의 화면 전환 애니메이션을 유지한다.
- A4 한국어·외국어 메뉴판은 기존 V1 승인본을 변경하지 않는다.

## 기준 파일

- 메뉴 데이터: `content/menu-data.js`
- 렌더링: `src/app.js`
- 스타일: `src/styles.css`
- 버전 메타데이터: `menu-version.json`

Git 태그 `v1.2`는 이 확정 상태를 가리킨다.
