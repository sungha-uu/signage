# 섹시한 꾼만두 디지털 메뉴판 디자인·구현 명세

이 문서는 현재 메뉴판을 처음 보는 개발자나 다른 에이전트가 기존 소스 없이도 동일한 화면과 동작을 다시 만들 수 있도록 작성한 재현 명세다. 구현의 기준 해상도는 **1920×1080, 16:9 가로 화면**이며, 문서의 수치와 현재 `src/styles.css`, `src/app.js`, `content/menu-data.js`가 함께 최종 기준이다.

문서 기준 버전: `20260808-51`

---

## 1. 완성 결과의 핵심 조건

1. 메뉴판은 항상 16:9 가로 디자인으로 표시한다.
2. 화면은 2개다.
   - 1페이지: 대표 메뉴, 음식 사진 3장, 전체 가격표.
   - 2페이지: 왼쪽 메뉴판 50%, 오른쪽 영상 50%.
3. 웹/GitHub Pages에서는 페이지 전환과 영상 재생을 사용자가 직접 한다.
4. Windows EXE에서는 1페이지를 일정 시간 표시한 뒤 2페이지로 자동 전환하고 영상을 자동 재생한다. 영상이 끝나면 1페이지로 돌아간다.
5. 메뉴명, 가격, 보조 설명, 태그, 노출 여부는 `content/menu-data.js` 한 파일에서 관리한다.
6. 사진, 로고, 영상은 `content/assets/` 아래에서 별도 관리한다.
7. 화면의 가장 중요한 우선순위는 TV 원거리 가독성이다. 장식보다 큰 메뉴명, 지나치게 뭉치지 않는 굵기, 한 단계 얇은 가격, 정확한 정렬을 우선한다. 메뉴명과 가격 사이에는 저대비의 성긴 점선 리더를 사용한다.
8. 전체 인상은 따뜻한 코랄색 외곽, 크림색 본문, 검은 글자, 빨간 포인트의 수제 만두 전문점 메뉴판이다.

---

## 2. 프로젝트 파일 구조

```text
menu-board/
├─ index.html                     # 1920×1080 고정 무대를 화면에 맞게 축소하는 외부 프레임
├─ board.html                     # 실제 메뉴판 HTML 진입점
├─ menu_design.md                 # 본 디자인·구현 명세
├─ content/
│  ├─ menu-data.js                # 모든 메뉴/가격/문구/동작 데이터
│  └─ assets/
│     ├─ fonts/                   # 메뉴용 폰트 자산
│     ├─ images/                  # 로고와 음식 사진
│     └─ video/menu-video.mp4     # 2페이지 영상
├─ src/
│  ├─ app.js                      # 1·2페이지 HTML 생성과 전환/영상 제어
│  ├─ styles.css                  # 실제 메뉴판 디자인 전체
│  ├─ frame.js                    # 1920×1080 무대 축소와 모바일 페이지 버튼
│  └─ frame.css                   # 외부 프레임/모바일 버튼 디자인
├─ electron/
│  └─ main.cjs                    # Windows EXE 진입점
└─ scripts/
   └─ build-release.cjs           # 휴대용 배포 폴더 조립
```

### 렌더링 순서

```text
index.html
  └─ 1920×1080 iframe
       └─ board.html
            ├─ content/menu-data.js
            ├─ src/app.js
            └─ src/styles.css
```

`index.html`의 역할은 디자인을 재배치하는 것이 아니라 1920×1080 무대를 비율 그대로 축소하는 것이다. 따라서 휴대폰에서도 세로형으로 재배열하지 않고 TV와 같은 가로 메뉴판을 축소해서 보여준다.

---

## 3. 색상 토큰

색상은 반드시 아래 값을 사용한다.

| 용도 | 변수/값 | 설명 |
|---|---:|---|
| 기본 코랄 | `--coral: #ed553b` | 외곽 배경과 일반 강조 |
| 진한 코랄 | `--coral-deep: #d94730` | 작은 강조 문구 |
| 연한 코랄 | `--coral-soft: #fff0eb` | 베스트 메뉴 카드 배경 |
| 가격 빨강 | `--price-red: #f04438` | 9,000원 핵심 가격 |
| 본문 검정 | `--ink: #171918` | 메뉴명, 제목, 가격 |
| 보조 회색 | `--muted: #66645f` | 설명, 부제목 |
| 크림 배경 | `--cream: #fffdf8` | 큰 흰색 메뉴판 영역 |
| 테두리 | `--line: #ded9d0` | 카드 경계 |
| 여름 태그 파랑 | `--blue: #37a7d8` | `여름 한정` |
| 영상 배경 | `#131313` | 영상 로딩/레터박스 배경 |
| 행 구분선 | `#ece7df` | 메뉴 행 사이의 얇은 실선 |
| 점선 리더 | `rgba(102,100,95,.32)` | 메뉴명에서 가격으로 시선을 연결하는 성긴 점 |

외곽 빨간 배경은 단색이 아니라 다음 조합이다.

```css
background:
  radial-gradient(circle at 12% 10%, rgba(255,255,255,.1), transparent 24%),
  linear-gradient(135deg, #f45d40 0%, #ed553b 52%, #dc442f 100%);
```

---

## 4. 타이포그래피

### 기본 글꼴

```css
font-family: "Pretendard Local", "Pretendard Variable", "Noto Sans KR", "Malgun Gothic", sans-serif;
```

- `content/assets/fonts/PretendardVariable.woff2`를 로컬로 포함해 웹과 오프라인 EXE에서 동일한 글꼴을 사용한다.
- 글꼴은 Pretendard v1.3.9이며 SIL Open Font License 전문을 `content/assets/fonts/Pretendard-LICENSE.txt`에 함께 보관한다.
- 메뉴명은 `font-weight: 760`, 카드 제목은 `780`, 가격은 `600`이다. 메뉴명과 금액의 굵기 차이로 정보 위계를 만든다.
- 메뉴명에 텍스트 그림자나 인위적인 좌우 획 보강을 적용하지 않는다. 큰 크기는 유지하되 원래 글리프의 여백을 살린다.
- 장식용 기울임꼴이나 손글씨체는 사용하지 않는다.
- `MenuSquare`, `MenuSquareOutline` 폰트 파일은 자산으로 포함되어 있지만 현재 핵심 제목과 메뉴명은 Pretendard를 사용한다.

### 글자 크기 기준

| 요소 | 1920×1080 기준 |
|---|---:|
| 1페이지 베스트 메뉴명 | `43px / 780` |
| 1페이지 베스트 가격 | `54px / 700` |
| 1페이지 만두 카드 제목 | `44px / 780` |
| 1페이지 만두 메뉴명 | `45px / 760` |
| 1페이지 만두 가격 | `34px / 600` |
| 1페이지 별미·면 카드 제목 | `44px / 780` |
| 1페이지 별미·면 메뉴명 | `45px / 760` |
| 1페이지 별미·면 가격 | `34px / 600` |
| 1페이지 보조 설명 | `16px / 650` |
| 2페이지 베스트 메뉴명 | `50px / 780` |
| 2페이지 베스트 가격 | `54px / 700` |
| 2페이지 카드 제목 | `40px / 780` |
| 2페이지 메뉴명 | `43px / 760` |
| 2페이지 가격 | `34px / 600` |
| 2페이지 보조 설명 | `14px / 650` |

1페이지 베스트 제목의 `고기만두`, `+`, `비빔야채` 세 줄은 같은 제목 블록을 기준으로 모두 가운데 정렬한다. `+`만 별도 좌표로 옮기지 않아야 두 메뉴명 사이의 정확한 중앙축에 위치한다.

1·2페이지 세트 조합의 `비빔야채`는 만두 카드의 비빔야채와 같은 코랄색 `#ed553b`을 사용한다. 세트 가격 `9,000원`은 더 진한 빨간색 `#d9362b`으로 구분한다. 1페이지에서는 비빔야채 아래 가격의 위쪽 여백을 `.38em`으로 두어 두 정보가 붙어 보이지 않게 한다.

메뉴명과 가격은 한 줄을 유지한다. 가격은 항상 오른쪽 끝에 붙고 `white-space: nowrap`을 적용한다.

---

## 5. 공통 1920×1080 무대

### 외부 프레임

- 디자인 크기: `1920×1080`.
- 실제 브라우저 크기에 대한 배율:

```js
scale = Math.min(window.innerWidth / 1920, window.innerHeight / 1080)
```

- 무대는 화면 중앙에 고정한다.
- 가로세로 비율이 다르면 남는 영역은 짙은 갈색/검정 배경으로 처리한다.
- 모바일도 동일한 1920×1080 무대를 축소한다.

### 메뉴판 화면 공통 여백

```css
.screen {
  padding: 28px 36px 12px;
}
```

1920×1080 기준:

- 좌우 외곽 여백: 각각 `36px`.
- 위 외곽 여백: `28px`.
- 아래 외곽 여백: `12px`.
- 하단 띠 높이: `30px`.
- 큰 크림색 보드: `1848×1010px`, 좌표 `x=36, y=28`.
- 하단 띠: 좌표 `x=36, y=1038`, 크기 `1848×30px`.

### 큰 크림색 보드

```css
.board {
  background: #fffdf8;
  border-radius: clamp(24px, 2.5vw, 52px);
  overflow: hidden;
  box-shadow: 0 24px 70px rgba(91,33,20,.2);
}
```

- 모서리는 크게 둥글다.
- 보드 안쪽 가장자리에 반투명 흰색 1px 테두리를 한 번 더 둔다.
- 모든 카드는 크림색 보드 안에 완전히 들어가야 한다.

### 공통 하단 띠

3열 그리드다.

```css
grid-template-columns: 1fr auto 1fr;
align-items: center;
min-height: 30px;
```

- 왼쪽 문구: 왼쪽 정렬.
- 로고: 화면의 정확한 가로 중앙.
- 오른쪽 문구: 오른쪽 정렬.
- 세 요소의 세로 중심 오차는 `0px`이어야 한다.
- 로고 이미지 높이 `28px`, 로고 래퍼 높이 `30px`.
- PNG 투명 여백 때문에 그림이 위로 떠 보이지 않도록 실제 로고 이미지만 `3px` 아래로 시각 보정한다.
- 하단에는 캐릭터와 `섹시한 꾼만두` 글자가 모두 포함된 전체 `logo.png`를 그대로 사용한다. 카드 제목용 크롭 아이콘과 혼동하거나 교체하지 않는다.
- 로고에 별도 흰색 배경을 넣지 않는다.
- 1페이지 왼쪽: `매일 직접 빚는 수제만두 전문점`.
- 1페이지 오른쪽: `맛있는 한 끼, 정성껏 준비했습니다.`.
- 2페이지 왼쪽: `FRESH & HANDMADE`.
- 2페이지 오른쪽: `매일 정성껏 빚고, 깨끗하게 튀깁니다.`.

---

## 6. 1페이지 상세 설계

### 전체 보드 그리드

```css
.board--static {
  grid-template-rows: repeat(2, minmax(0, 1fr));
  gap: 18px;
  padding: 28px;
}
```

1920×1080 기준 보드 내부:

- 안쪽 전체 영역: `1792×954px`.
- 상단 음식 행: `468px`.
- 하단 메뉴 카드 행: `468px`.
- 상·하 행 사이 간격: `18px`.
- 상단과 하단의 카드 위·아래 기준선은 각각 완전히 일치한다.

### 상단 음식 4개 행

```css
.static-photo-row {
  grid-template-columns: 1.25fr 1fr 1fr;
  gap: 18px;
}
```

- 왼쪽은 `고기만두+비빔야채 대표 카드`, 오른쪽은 사진 3장을 묶은 흰색 갤러리 박스다.
- 대표 카드와 흰색 갤러리 박스의 상단 y, 하단 y, 높이는 동일하다.
- 대표 카드는 첫 번째 열을 차지해 약 `675.4px`, 갤러리 박스는 2·3열을 합쳐 약 `1098.6px`다.
- 상단과 하단이 같은 열 비율을 사용하므로 대표 카드/갤러리 사이 18px 틈은 만두/별미 카드 사이 18px 틈과 x좌표가 정확히 같다. 두 틈은 상·하 행 사이 18px 틈과 만나 완전한 십자 모양을 만든다.
- 갤러리 박스는 흰색 배경, `1px solid #ded9d0`, `28px` 모서리, `14px` 안쪽 여백을 사용한다.
- 갤러리 안 사진 3장은 `14px` 간격의 3열이며 높이와 상·하 기준선이 완전히 같다. 각 사진 모서리는 `18px`다.
- 사진 자체 높이는 바깥 박스보다 위·아래 각각 `14px` 작게 하여 흰색 테두리 공간이 보이게 한다.
- 일반 사진은 `object-fit: cover`를 사용한다.
- 탕수육 사진은 음식과 접시가 중심을 채우되 약간의 여백도 보이도록 `transform: scale(1.58)`를 적용한다. 이전 1.75배 상태에서 약 10% 축소한 값이다.

#### 베스트 인기 메뉴 카드

- 배경: `#fff0eb`.
- 테두리: `1px solid #f3c2b6`.
- 모서리: `28px`.
- 내부 열: 텍스트 `29%`, 사진 `71%`.
- 텍스트 왼쪽/오른쪽 패딩: `20px`.
- 태그: `인기`, 코랄 배경, 흰색 글자, `15px/800`.
- 소제목: `베스트 인기 메뉴`, 진한 코랄, `19px/780`.
- 메뉴명은 세 줄 구조다.

```text
고기만두
   +
비빔야채
```

- `+`는 빨강이고 텍스트 열의 수평 중앙에 놓는다.
- 가격 `9,000원`은 `54px`, `#d9362b`이며 비빔야채 아래 `20.52px` 정도의 간격을 둔다.
- 사진 래퍼 여백: 위/오른쪽/아래 `18px`, 왼쪽 `6px`.
- 사진은 `object-fit: cover`, `scale(1.025)`.

### 하단 메뉴 카드 3개 행

```css
.static-menu-row {
  grid-template-columns: 1.25fr 1fr 1fr;
  gap: 18px;
}
```

- 왼쪽부터 `만두 메뉴`, `별미 메뉴`, `면 메뉴`다.
- 세 카드 높이는 모두 `468px`로 완전히 동일하다.
- 만두 메뉴는 2열 목록을 수용하므로 약 `675.4px`, 별미와 면 메뉴는 각각 약 `540.3px`다.
- 세 카드 패딩은 위 `18px`, 좌우 `22px`, 아래 `14px`로 통일한다.
- 세 카드 제목선의 y좌표와 제목 크기를 동일하게 맞춘다.

#### 만두 메뉴 카드

- 흰색 배경.
- 테두리: `1px solid #ded9d0`.
- 모서리: `24px`.
- 패딩: 위 `18px`, 좌우 `22px`, 아래 `14px`.
- 머리말 아래 검은 선: `3px`.
- 제목 위의 `HANDMADE` 영문 장식은 사용하지 않는다.
- 1페이지의 세 카드 제목은 모두 `44px/780`, `line-height: 1`, `letter-spacing: -.035em`으로 동일하다.
- 1·2페이지 만두 카드 제목 바로 오른쪽에 `꾼만두/찐만두`를 괄호 없이 검은색으로 표시한다. 안내 문구는 굵기 `800`, 줄바꿈 없음, 제목 하단 정렬(`align-self: flex-end`)로 고정한다. 1페이지는 기존 우측 보조문구 `매일 직접 빚는 수제만두`를 반드시 유지하며 안내를 제목의 `0.5em`, 왼쪽 간격 `.38em`으로 표시한다. 2페이지 안내는 제목의 `0.6em`, 왼쪽 간격 `.55em`을 사용한다.
- 메뉴 목록: 2열 × 4행.
- 열 간격: `28px`.
- 각 메뉴 행은 위·아래 패딩 `0`, `align-items: center`이며 메뉴명과 가격의 세로 중심을 행의 정확한 중앙에 둔다.
- 설명이 있는 메뉴도 메뉴명 자체의 중앙축을 바꾸지 않는다. 설명은 절대 배치해 행 높이 계산에서 분리한다.
- 1·2페이지 만두 메뉴 행은 `메뉴명 | 가격`의 2열 CSS Grid를 사용한다. 첫 열은 `minmax(0, 1fr)`, 두 번째 열은 `max-content`다.
- 만두 메뉴의 보조 설명은 1페이지 `top: calc(50% + 1.6em)`, 2페이지 `top: calc(50% + 1.3em)`에 놓는다. 2페이지 73.5px 행 안에서 설명 하단 여백이 약 2.2px 남아야 한다.
- `비빔야채`처럼 설명이 없는 행도 메뉴명과 가격이 같은 중앙축에 위치한다.

배치 순서는 데이터 배열을 CSS Grid 자동 배치 순서로 채운 결과다.

```text
1행: 모듬만두 | 고기만두
2행: 깻잎만두 | 땡초만두
3행: 새우만두 | 탕수만두
4행: 비빔야채 | 빈 칸
```

`비빔야채`는 메뉴명과 가격을 코랄색으로 강조한다.

#### 별미/면 메뉴 카드

- 두 카드 너비는 동일하다.
- 카드 사이 간격: `18px`.
- 각 카드 너비 약 `540.3px`.
- 카드 패딩은 만두 카드와 동일하게 위 `18px`, 좌우 `22px`, 아래 `14px`.
- 카드 제목 아래 검은 선: `3px`.
- 제목 위의 빨간 `MENU` 영문 장식은 사용하지 않는다.
- 메뉴 목록은 남은 높이를 동일 행으로 나눈다.
- 메뉴명과 가격이 각 행의 세로 중앙에 놓인다.
- 태그는 메뉴명 왼쪽 공간을 차지하지 않고 메뉴명 위에 절대 배치한다. 1페이지에서는 행 왼쪽 위 `top: 3px`, `left: 4px`, 높이 `18px`, 글자 `12px/750`으로 두며 메뉴명과 약 `2.4px` 간격을 남긴다.
- 태그가 있는 메뉴와 없는 메뉴의 메뉴명 시작 x좌표는 같아야 한다.

---

## 7. 2페이지 상세 설계

### 전체 보드 그리드

```css
.board--video {
  grid-template-columns: 1fr 1fr;
  gap: 26px;
  padding: 28px;
}
```

- 메뉴판과 영상 비율은 정확히 `50:50`.
- 안쪽 전체 너비 `1792px`에서 중앙 간격 `26px`를 뺀 뒤 각각 약 `883px`.
- 오른쪽 영상은 보드의 전체 안쪽 높이 `954px`를 사용한다.

### 왼쪽 메뉴판

왼쪽은 추천 조합 카드와 메뉴 카드 그룹으로 나눈다.

```css
.video-menu {
  grid-template-rows: auto 1fr;
  gap: 18px;
}
```

#### 추천 조합 카드

- 높이 약 `111.1px`.
- 배경/테두리는 1페이지 베스트 카드와 동일하다.
- `BEST COMBO`를 `고기만두 + 비빔야채` 글자 바로 위에 표시한다.
- 제목 행에는 `인기` 태그 → `고기만두 + 비빔야채` → `9,000원` 순서로 표시한다.
- 제목: `고기만두 + 비빔야채`, `50px/780`.
- `인기` 태그, 제목, 가격은 서로 분리된 좌우 영역이 아니라 하나의 3열 묶음으로 구성한다. 세 요소 사이 간격은 `18px`이며 전체 묶음을 추천 카드의 가로·세로 정중앙에 둔다.
- 가격: `9,000원`, `54px/700`, `#f04438`.

#### 메뉴 카드 그룹

```css
.video-menu__groups {
  grid-template-columns: 1fr 1fr;
  grid-template-rows: 48fr 52fr;
  gap: 18px;
}
```

- 상단 만두 카드가 두 열을 모두 차지하며 제목은 `만두 메뉴`로 표시한다.
- 하단 왼쪽은 별미, 하단 오른쪽은 면 메뉴다.
- 추천 카드→만두 카드, 만두 카드→하단 카드, 별미→면 카드의 간격은 모두 정확히 `18px`.
- 최종 실측:
  - 만두 카드 높이 약 `387.3px`.
  - 별미 메뉴 카드 높이 약 `419.6px`.
  - 면 메뉴 카드 높이 약 `419.6px`.
  - 상·하 카드 높이 차이 약 `32.3px`.
- 태그가 있는 하단 카드 목록에는 `5px`의 위쪽 내부 여백을 둔다.
- 인기 태그와 카드 제목 아래 검은 선 사이 실측 간격은 약 `7.8px`.

#### 2페이지 메뉴 행

- 카드 패딩 `20px`.
- 카드 제목 아래 검은 선 `3px`.
- 만두 목록: 2열 × 4행, 열 간격 `28px`.
- 카드 제목 `40px/780`, 메뉴명 `43px/760`, 가격 `34px/600`, 보조 설명 `14px/650`.
- 태그는 메뉴명 위에 절대 배치한다. 2페이지에서는 행 왼쪽 위 `top: 0`, `left: 4px`, 높이 `15px`, 글자 `11px/750`으로 두며 메뉴명과 약 `2.4px` 간격을 남긴다.
- 태그 때문에 메뉴명이 오른쪽으로 밀리면 안 된다.
- 설명이 있는 메뉴도 메뉴명은 행 중앙에 그대로 두며 별도의 아래쪽 이동을 적용하지 않는다.
- 태그, 메뉴명, 가격이 카드 경계를 넘으면 안 된다.

### 오른쪽 영상

- 배경 `#131313`.
- 모서리 `clamp(18px, 1.65vw, 32px)`.
- 영상은 `width: 100%`, `height: 100%`, `object-fit: cover`.
- 영상 제목 라벨은 왼쪽 아래 반투명 검정 알약 형태다.
- 웹 모드에서는 영상 기본 컨트롤을 표시하고 자동 재생하지 않는다.
- EXE 모드에서는 컨트롤을 숨기고 자동 재생한다.
- 현재 영상 파일 길이: 약 `1분 55초`.
- 오디오는 기본 음소거다.

---

## 8. 메뉴 행과 태그 공통 규칙

### 카드 제목과 아이콘

- 1·2페이지 제목은 `만두 메뉴`, `별미 메뉴`, `면 메뉴`로 통일한다.
- `만두 메뉴`: `logo.png` 원본을 수정하지 않고 CSS의 고정 폭 크롭 영역으로 왼쪽 만두 캐릭터 두 개만 표시한다.
- 카테고리 제목은 모두 `아이콘 → 제목` 순서로 배치한다.
- `별미 메뉴`: `✨️` 아이콘.
- `면 메뉴`: `🍜` 아이콘.
- 카드 제목용 만두 캐릭터 크롭은 폭 `2.05em`, 높이 `1em`으로 두 캐릭터를 온전히 담고 원본 이미지의 종횡비를 유지한다. 오른쪽 흰 만두를 잘라내거나 폭을 강제로 늘려 찌그러뜨리지 않는다. 이 크롭은 메뉴 제목에서만 사용한다. 하단 빨간 띠에는 반드시 캐릭터와 상호 글자가 모두 포함된 전체 로고를 유지한다.
- 1페이지 카드 제목은 모두 `44px`, 2페이지 카드 제목은 모두 `40px`이다. 페이지 간 크기는 달라도 되지만 같은 페이지 안의 세 제목은 글꼴·크기·행간·자간까지 동일해야 한다.

메뉴 한 행의 DOM 구조는 다음 형태다.

```html
<li class="menu-row">
  <div class="menu-row__copy">
    <div class="menu-row__title-line">
      <span class="badge">인기</span>
      <strong class="menu-row__name">콩나물어묵</strong>
    </div>
    <small class="menu-row__description">1인분 4개</small>
  </div>
  <strong class="menu-row__price">5,000원</strong>
</li>
```

### 반드시 지켜야 할 정렬 규칙

1. 메뉴명과 가격은 같은 행 중심에 위치한다.
2. 메뉴명과 가격 사이에는 `8px` 간격의 성긴 점선 리더를 둔다. 점 색은 `rgba(102,100,95,.32)`이며 높이는 `2px`다.
3. 점선 리더는 남는 폭에 맞춰 자동으로 짧아지거나 길어지며 최소 폭은 일반 행 `14px`, 만두 2열 행 `12px`다. 메뉴 행 사이는 `1px solid #ece7df`의 매우 옅은 실선으로 구분한다.
4. 태그는 메뉴명 위에 겹치지 않게 놓되 메뉴명의 x좌표를 밀지 않는다.
5. 태그가 있는 메뉴와 없는 메뉴의 메뉴명 시작 위치는 동일하다.
6. 가격은 행의 끝에 고정해 모든 가격의 오른쪽 끝이 카드 기준으로 맞아야 한다.
7. 마지막 메뉴 행에는 아래쪽 구분선을 표시하지 않는다.

### 태그

- `인기`: 코랄 배경 `#ed553b`, 흰색 글자.
- `여름 한정`: 파란 배경 `#37a7d8`, 흰색 글자.
- 모서리는 작은 둥근 사각형.
- 태그 글자는 `750`으로 사용하고 작은 크기에서도 뭉치지 않게 한다.

---

## 9. 현재 메뉴 데이터

### 베스트 조합

| 태그 | 메뉴 | 가격 |
|---|---|---:|
| 인기 | 고기만두 + 비빔야채 | 9,000원 |

### 만두

| 메뉴 | 가격 | 설명 | 강조 |
|---|---:|---|---|
| 모듬만두 | 7,000원 | 고기2 · 깻잎1 · 땡초1 · 새우1 | 없음 |
| 고기만두 | 7,000원 | 1인분 5개 | 없음 |
| 깻잎만두 | 7,000원 | 1인분 5개 | 없음 |
| 땡초만두 | 7,000원 | 1인분 5개 | 없음 |
| 새우만두 | 7,000원 | 1인분 5개 | 없음 |
| 탕수만두 | 9,000원 | 1인분 5개 | 없음 |
| 비빔야채 | 2,000원 | 없음 | 코랄 글자 |

### 별미 메뉴

| 메뉴 | 가격 | 설명 | 태그 |
|---|---:|---|---|
| 옛날 탕수육 | 12,000원 | 없음 | 인기 |
| 유린 탕수육 | 14,000원 | 없음 | 없음 |
| 콩나물어묵 | 5,000원 | 1인분 4개 | 인기 |
| 음료수 | 2,000원 | 콜라 · 사이다 | 없음 |

### 면 메뉴

| 메뉴 | 가격 | 설명 | 태그 |
|---|---:|---|---|
| 냉메밀 | 7,000원 | 없음 | 여름 한정 |
| 냄비우동 | 5,000원 | 없음 | 없음 |
| 얼큰우동 | 6,000원 | 없음 | 없음 |
| 비빔우동 | 6,000원 | 없음 | 없음 |

---

## 10. 메뉴 데이터 스키마

`content/menu-data.js`의 메뉴 항목 형식:

```js
{
  name: "탕수만두",       // 필수: 화면에 표시할 메뉴명
  price: 9000,            // 필수: 숫자로 입력, 화면에서 9,000원으로 변환
  description: "1인분 5개", // 선택: 메뉴명 아래 작은 설명
  badge: "인기",          // 선택: 인기/여름 한정 등의 태그
  accent: true,           // 선택: true면 메뉴명과 가격을 코랄색으로 강조
  visible: true           // false면 1·2페이지 모두 숨김
}
```

### 데이터 수정 원칙

- 가격은 `"7,000원"` 같은 문자열이 아니라 `7000` 숫자로 쓴다.
- 메뉴를 숨길 때 삭제하지 말고 `visible: false`를 사용할 수 있다.
- 데이터 배열 순서는 화면 배치 순서에 직접 영향을 준다.
- 현재 만두 카드의 최대 안정 수용량은 7개다. 8번째는 빈 오른쪽 아래 칸까지 사용할 수 있지만 글자 길이와 설명 유무를 다시 검증해야 한다.
- 별미와 면 메뉴는 각각 4개를 기준으로 설계했다. 추가 시 카드 행 높이와 태그 간격을 다시 조정해야 한다.

---

## 11. 이미지·영상 자산 규격

| 파일 | 현재 크기 | 화면 용도 | 표시 방식 |
|---|---:|---|---|
| `logo.png` | 510×103 | 하단 중앙 로고 | contain, 높이 28px |
| `mandu-bibim.png` | 587×472 | 1페이지 베스트/영상 포스터 | cover |
| `tangsuyuk.jpeg` | 853×1280 | 1페이지 사진 1 | cover + 1.58배 확대 |
| `fishcake.png` | 271×271 | 1페이지 사진 2 | cover |
| `cold-noodle.png` | 283×283 | 1페이지 사진 3 | cover |
| `menu-video.mp4` | 약 24.5MB, 1:55 | 2페이지 영상 | cover, 기본 음소거 |

### 사진 교체 원칙

1. 파일명을 유지하면 코드 수정 없이 교체할 수 있다.
2. 음식의 중심이 사진 중앙에 오도록 촬영/크롭한다.
3. 중요한 음식이 프레임 가장자리에 닿지 않게 한다.
4. `object-fit: cover` 때문에 가장자리는 잘릴 수 있다.
5. 탕수육 사진은 CSS에서 1.58배 확대되므로 교체 사진이 이미 크게 촬영되었다면 확대값을 다시 조정한다.
6. 로고 PNG는 투명 배경을 유지한다. 흰색 사각형 배경을 넣지 않는다.

---

## 12. 웹 모드와 EXE 모드 동작

### 웹 모드

접속 주소 예:

```text
https://sungha-uu.github.io/signage/
```

- `autoRotate: false`.
- `videoAutoplay: false`.
- `videoControls: true`.
- 오른쪽 아래 페이지 버튼 또는 모바일 하단 버튼으로 이동한다.
- 영상 재생 버튼을 사용자가 누른다.

### EXE 사이니지 모드

내부 주소:

```text
index.html?mode=signage
```

- `autoRotate: true`.
- `videoAutoplay: true`.
- `videoControls: false`.
- 페이지 전환 컨트롤을 숨긴다.
- 1페이지 표시 시간은 `staticDurationSeconds`이며 현재 `30초`.
- 30초 후 2페이지로 이동하고 영상을 처음부터 자동 재생한다.
- 영상 `ended` 이벤트가 발생하면 1페이지로 돌아간다.
- 1페이지로 돌아갈 때 영상은 일시정지하고 재생 위치를 0으로 초기화한다.
- EXE 창은 전체 화면/키오스크로 실행한다.
- 디스플레이 절전 진입을 방지한다.

#### EXE 2종 배포 규칙

- `섹시한 꾼만두 메뉴판.exe`: `effects` 쿼리가 없는 정적 버전. 계절 입자와 화면 전환 애니메이션을 모두 사용하지 않는다.
- `섹시한 꾼만두 메뉴판 - 여름 애니메이션.exe`: `effects=summer`를 자동 추가한다.
- 여름 버전은 TV에서도 식별 가능한 크기와 명도의 CSS 잎사귀 18개가 왼쪽에서 오른쪽으로 이동하고, 화면 모서리에 햇빛·초록빛 글로를 표시한다. `effects=summer`를 명시적으로 선택한 경우에는 Windows의 모션 감소 설정과 무관하게 효과를 재생한다.
- 잎사귀 레이어는 `pointer-events: none`, 낮은 불투명도를 사용하며 메뉴 조작과 가독성을 방해하지 않는다.
- 두 EXE 모두 페이지가 바뀔 때 `signage-screen-in` 전환을 `0.72초` 적용한다. 정적 버전은 상시 잎 효과 없이 화면 전환 효과만 사용하고, 여름 버전은 화면 전환과 상시 잎 효과를 함께 사용한다.
- 두 EXE는 같은 바이너리를 복사해 만들고, Electron은 `PORTABLE_EXECUTABLE_FILE`의 파일명에 `여름 애니메이션`이 포함됐는지 확인해 효과를 선택한다.
- 두 버전은 동일한 외부 `content` 폴더를 사용한다.

---

## 13. 반응형·모바일 규칙

- 핵심 디자인은 재배열하지 않고 1920×1080을 비율대로 축소한다.
- 외부 프레임이 화면 중앙에 배치하고 남는 영역은 어두운 배경으로 둔다.
- 화면 너비가 `1100px` 이하이면 외부 하단에 `메뉴판 1`, `메뉴판 2 · 영상` 버튼을 표시한다.
- 세로 휴대폰에서는 가로 전환 안내를 보여준다.
- 모바일에서도 웹 모드이므로 자동 순환/자동 재생하지 않는다.

`styles.css` 안의 1100px 이하 규칙은 `board.html`을 프레임 없이 직접 열 때의 안전장치다. 정상 서비스는 `index.html` 프레임을 통하므로 실제 디자인은 항상 1920×1080이다.

---

## 14. 픽셀 단위 검수 기준

1920×1080 내부 프레임에서 다음 값을 확인한다.

### 공통

- `body.scrollWidth - body.clientWidth = 0`.
- `body.scrollHeight - body.clientHeight = 0`.
- 하단 띠 높이 `30px`.
- 하단 왼쪽 문구, 로고, 오른쪽 문구의 중심 y 오차 `0px`.

### 1페이지

- 만두·별미·면 카드 높이 모두 약 `468px`.
- 세 카드의 상단·하단 기준선 오차 `0px`.
- 사진 순서: 탕수육 → 콩나물어묵 → 냉메밀.
- 탕수육 사진만 확대되어 보여야 한다.
- `탕수만두` 아래 `1인분 5개`가 표시되어야 한다.
- `콩나물어묵` 아래 `1인분 4개`가 표시되어야 한다.

### 2페이지

- 메뉴판/영상 너비 비율 `50:50`.
- 추천 카드와 만두 카드 사이 `18px`.
- 만두 카드와 하단 카드 사이 `18px`.
- 별미와 면 카드 사이 `18px`.
- 만두 카드 높이 약 `387.3px`.
- 별미/면 카드 높이 약 `419.6px`.
- 상하 카드 높이 차이 약 `32.3px`.
- 첫 인기 태그와 카드 제목선 사이 약 `7.8px`.
- 모든 태그는 카드 안에 있고 메뉴명과 겹치지 않아야 한다.

---

## 15. 동일 화면 재구축 절차

1. `index.html`에 1920×1080 iframe 무대를 만든다.
2. `board.html`에서 메뉴 데이터 → 앱 스크립트 순으로 로드한다.
3. 3장의 사진과 로고, 영상 파일을 자산 경로에 둔다.
4. 3절의 색상 토큰을 선언한다.
5. 공통 화면 패딩과 30px 하단 띠를 먼저 구현한다.
6. 1페이지를 동일 높이의 상·하 2행 그리드로 만든다.
7. 상·하단 모두 `1.25:1:1` 열 기준을 사용한다. 상단 갤러리는 2·3열을 합쳐 차지하고 하단에는 메뉴 카드 3개를 각각 둔다.
8. 2페이지를 `50:50`으로 만들고 왼쪽 카드 그룹을 `48:52`로 만든다.
9. 메뉴 행을 `메뉴명 | 성긴 점선 리더 | 가격` 구조로 만들고 점선이 남는 폭만 유동적으로 차지하게 한다.
10. 태그는 메뉴명 위에 absolute로 배치해 메뉴명 x좌표가 변하지 않게 한다.
11. 데이터로 1·2페이지를 동시에 렌더링한다.
12. 웹/EXE 쿼리 모드에 따라 자동 순환과 영상 제어를 분리한다.
13. 14절의 실측값으로 검수한다.

---

## 16. 디자인 변경 시 안전 규칙

- 메뉴 수가 바뀌면 먼저 데이터만 수정하고 1920×1080에서 넘침을 검사한다.
- 카드 높이 조정은 px 고정값보다 현재의 `fr` 비율을 조정한다.
- 1페이지 만두·별미·면 세 카드는 반드시 같은 높이와 같은 상·하 기준선을 유지한다.
- 2페이지 별미/면 카드가 만두 카드보다 약 32px 높아야 한다.
- 하단 띠 높이 변경은 1·2페이지에 공통 적용한다.
- 태그 여백을 늘릴 때 메뉴명을 오른쪽으로 밀지 않는다.
- 영상 비율은 사용자의 최종 결정인 50:50을 유지한다.
- 로고를 상단에 다시 넣지 않는다. 현재 로고는 하단 중앙에만 사용한다.
- 웹 모드에 자동 순환이나 영상 자동 재생을 추가하지 않는다.

---

## 17. 최종 체크리스트

- [ ] 1920×1080에서 모든 카드가 보드 안에 들어간다.
- [ ] 1페이지 만두·별미·면 카드 높이와 상·하 기준선이 동일하다.
- [ ] 2페이지 카드 간격이 모두 18px다.
- [ ] 2페이지 하단 카드가 만두 카드보다 약 32px 높다.
- [ ] 인기/여름 한정 태그가 메뉴명 위에 있고 x좌표를 밀지 않는다.
- [ ] 가격 오른쪽 끝이 카드별로 정렬된다.
- [ ] 점선 리더가 낮은 대비와 8px 간격으로 메뉴명·가격을 연결하며 글자보다 먼저 눈에 띄지 않는다.
- [ ] 하단 3개 요소가 빨간 띠의 수직 중앙에 놓인다.
- [ ] 로고 배경이 투명하다.
- [ ] 웹에서는 수동 전환/수동 재생이다.
- [ ] EXE에서는 1페이지 30초 표시 후 자동 전환/자동 재생/영상 종료 후 복귀다.
- [ ] 정적 EXE에는 잎사귀가 없고 `signage-screen-in` 화면 전환만 적용된다.
- [ ] 여름 EXE에는 화면에 표시되는 잎사귀 18개와 `signage-screen-in` 전환이 적용된다.
- [ ] 외부 `content` 폴더를 수정하면 EXE 재빌드 없이 메뉴가 바뀐다.
